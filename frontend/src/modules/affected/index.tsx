import React, {useState} from 'react';
export const AffectedView: React.FC = () => {
  const [filter,setFilter]=useState('high');
  return <div><h2>AFFECTED - Affected - individuals, needs, triage, f</h2><p>individuals</p></div>
};
export default AffectedView;
